#include <bits/stdc++.h>
using namespace std;
 
#ifdef LOCAL
#include "debug.h"
#else
#define dbg(...)
#endif

int main(){	
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    #ifndef LOCAL
	freopen("leftout.in", "r", stdin);
	freopen("leftout.out", "w", stdout);
    #endif
    
    return 0;
}